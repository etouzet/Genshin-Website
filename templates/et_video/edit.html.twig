{% extends 'base.html.twig' %}

{% block title %}Edit Video{% endblock %}

{% block body %}
    {% if app.user %}
    {% if app.user.getRoles[0] == "ROLE_ADMIN" %}
    <h1>Edit Video</h1>

    {{ include('et_video/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('e_t_video_index') }}">back to list</a>

    {{ include('et_video/_delete_form.html.twig') }}
    {% endif %}
    {% endif %}
{% endblock %}
