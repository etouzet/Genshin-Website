{% extends 'base.html.twig' %}

{% block title %}Video{% endblock %}

{% block body %}
    <h1>Video</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ video.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ video.Title }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ video.createdAt ? video.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ video.link }}</td>
            </tr>
            <tr>
                <th>Tag</th>
                <td>{{ video.tag }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('e_t_video_index') }}">back to list</a>

    <a href="{{ path('e_t_video_edit', {'id': video.id}) }}">edit</a>

    {{ include('et_video/_delete_form.html.twig') }}
{% endblock %}
