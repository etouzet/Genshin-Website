{% extends 'base.html.twig' %}

{% block title %}Edit Article{% endblock %}

{% block body %}
    {% if app.user %}
        {% if app.user.getRoles[0] == "ROLE_ADMIN" %}
    <h1>Edit Article</h1>

    {{ include('et_article/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('e_t_article_index') }}">back to list</a>

    {{ include('et_article/_delete_form.html.twig') }}
     {% endif %}
    {% endif %}
{% endblock %}
