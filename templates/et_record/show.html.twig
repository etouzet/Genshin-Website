{% extends 'base.html.twig' %}

{% block title %}Record{% endblock %}

{% block body %}
    <h1>Record</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ record.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ record.content }}</td>
            </tr>
            <tr>
                <th>Player</th>
                <td>{{ record.player }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('e_t_record_index') }}">back to list</a>

    <a href="{{ path('e_t_record_edit', {'id': record.id}) }}">edit</a>

    {{ include('et_record/_delete_form.html.twig') }}
{% endblock %}
