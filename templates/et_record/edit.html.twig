{% extends 'base.html.twig' %}

{% block title %}Edit Record{% endblock %}

{% block body %}
    {% if app.user %}
        {% if app.user.getRoles[0] == "ROLE_ADMIN" %}
    <h1>Edit Record</h1>

    {{ include('et_record/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('e_t_record_index') }}">back to list</a>

    {{ include('et_record/_delete_form.html.twig') }}
     {% endif %}
    {% endif %}
{% endblock %}
