{% extends 'base.html.twig' %}

{% block title %}ContactForm{% endblock %}

{% block body %}
    <h1>ContactForm</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact_form.id }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ contact_form.mail }}</td>
            </tr>
            <tr>
                <th>Object</th>
                <td>{{ contact_form.object }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ contact_form.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('e_t_constact_form_index') }}">back to list</a>

    <a href="{{ path('e_t_constact_form_edit', {'id': contact_form.id}) }}">edit</a>

    {{ include('et_constact_form/_delete_form.html.twig') }}
{% endblock %}
